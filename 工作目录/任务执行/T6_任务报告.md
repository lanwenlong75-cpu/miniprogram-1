# T6 · 菜谱详情页 — 任务报告

## 1. 基本执行信息

- **开发的分支**：feat/stage-zero-hardcoded-data
- **完成时间**：2026-05-09
- **调用的 Skills**：dev-builder

## 2. 任务核对 (Checklist)

| 编号 | 任务 | 结果 | 变动文件 |
|------|------|------|---------|
| 6.1 | 新建详情页四个文件 + 注册 | ✅ 已创建 detail.js/wxml/wxss/json，已在 app.json 注册 | detail.js, detail.wxml, detail.wxss, detail.json, app.json |
| 6.2 | 实现 onLoad 数据加载 | ✅ 按 ID 查找菜谱 + 从 globalData 读已选食材并标记 owned 状态 | detail.js |
| 6.3 | 实现封面 + 菜名 + 标签区 | ✅ 大图占位（image 组件 + binderror 降级占位背景）、菜名大字、分类/时间/难度三标签 | detail.wxml, detail.wxss |
| 6.4 | 实现食材清单 | ✅ 必选/可选分区，已有✅绿底、缺少⚪灰底、可选标注"可选"橙色徽章 | detail.wxml, detail.wxss |
| 6.5 | 实现做法步骤列表 | ✅ 带编号圆点的步骤列表，行高 1.8 宽松间距 | detail.wxml, detail.wxss |
| 6.6 | 实现外链复制按钮 | ✅ 底部小红书/抖音两个按钮，wx.setClipboardData 复制 + wx.showToast 提示 | detail.wxml, detail.js |

## 3. 实现详情

### 前端界面 关键视觉与交互

- **状态管理**：使用 Page.data 管理 loaded/notFound/coverError 状态。从 `app.globalData.selectedIngredients` 读取已选食材，在 onLoad 中深拷贝菜谱数据并标记每个食材的 owned 属性
- **视觉规范**：复用 T3 暖色调体系（背景 #FFF8F0、主色 #FF8C42、文字 #4A3B32、辅色 #8C7A6B、绿色 #5B8C5A）。食材卡片采用圆角矩形容器区分状态。步骤编号采用圆形橙底白字徽章。外链按钮小红书红底(#FF2442)、抖音黑底(#1E1E1E)
- **动画/微交互**：无特殊动画

### 数据与云函数 核心业务逻辑

- **数据库/实体变更**：无（使用本地 recipes.js 硬编码数据）
- **数据流向**：`recipes.js` → `require()` 读取 → onLoad 按 ID filter 查找 → 与 globalData 已选食材交叉标记 owned 属性 → setData 渲染
- **依赖注入**：通过 `getApp()` 获取全局实例

## 4. 验证与排错记录

- **本地编译验证**：N/A（微信开发者工具内编译）(✅ 通过)
- **开发时的错误及解决**：无

## 5. 任务追加

（无）

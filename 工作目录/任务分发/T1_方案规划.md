<!-- DISPATCH PACK · Leader 钉死，下游不得自行判断 -->
```yaml
task_type: feature
implementer_resources:
  必加载: [dev-builder]
  条件加载: []
reviewer_checks:
  scope_check: true
  quality_check: true
  ui_design_check: false
  ui_design_modules: []
constraints:
  - SDK 版本: 微信小程序 2.2.3+
  - 禁用 API: 无
  - 架构约束: 数据模块放在 miniprogram/data/ 目录下，导出为 CommonJS 模块
```
<!-- END DISPATCH PACK -->

# T1 · 准备 5 道菜的硬编码数据 — 方案规划

---

## 第一层：契约

### 验收标准
- [ ] `miniprogram/data/recipes.js` 文件存在，导出包含 5 道菜的数组
- [ ] `miniprogram/data/ingredient-tags.js` 文件存在，导出食材标签列表
- [ ] 每道菜包含完整字段：id、name、cover（占位路径）、category、cookTime、difficulty、essentialIngredients（必选食材数组，每项含 name + amount）、optionalIngredients（可选食材数组）、steps（4-5 步字符串数组）、searchLinks（含 xiaohongshu 和 douyin 两个 URL）
- [ ] 食材标签列表包含约 17 个食材，分为 `frequent`（高频）和 `more`（更多）两组

### 输出文件
- `miniprogram/data/recipes.js`
- `miniprogram/data/ingredient-tags.js`

### 红线约束
- 不修改任何已有文件
- 不引入外部依赖
- 不使用云开发 API

---

## 第二层：任务清单

| 编号 | 任务 | 完成标准 |
|------|------|---------|
| 1.1 | 创建 `miniprogram/data/recipes.js` | 5 道菜数据完整，字段齐全 |
| 1.2 | 创建 `miniprogram/data/ingredient-tags.js` | ~17 个食材标签，分 frequent/more 两组 |

---

## 第三层：任务追加

（无）

---

## 第四层：Scout 摘要

> 跳过（T1 为新建数据文件，不涉及现有代码阅读）

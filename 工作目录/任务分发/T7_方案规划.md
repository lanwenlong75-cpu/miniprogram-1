<!-- DISPATCH PACK · Leader 钉死，下游不得自行判断 -->
```yaml
task_type: ui_optimize
implementer_resources:
  必加载: [dev-builder]
  条件加载: [impeccable]
reviewer_checks:
  scope_check: true
  quality_check: true
  ui_design_check: true
  ui_design_modules: [impeccable critique, heuristics-scoring]
constraints:
  - SDK 版本: 微信小程序 2.2.3+
  - 禁止: 不修改 data/ 和 utils/ 目录、不使用云开发 API
  - 注意: 微信小程序 WXSS 不完全支持 CSS 自定义属性（var(--xxx)），需用 LESS/SCSS 变量思路或直接用类名复用。推荐方案：在 app.wxss 用注释定义色板，各页面直接使用色值
```
<!-- END DISPATCH PACK -->

# T7 · 全局样式与页面注册 — 方案规划

---

## 第一层：契约

### 验收标准
- [ ] `app.wxss` 添加注释色板定义（主色暖橙 #FF8C42、背景暖白 #FFF8F0、文字深棕灰 #4A3B32、成功绿 #5B8C5A、警告暖黄 #E8A840、边框 #EDE6DD、间距等）
- [ ] `app.json` 导航栏统一暖白底色：`navigationBarBackgroundColor: #FFF8F0`，标题文字深棕灰 `navigationBarTextStyle: black`（微信只支持 black/white）
- [ ] 三页注册确认：`pages/index/index`、`pages/search/search`、`pages/detail/detail` 均已在 `app.json` 的 `pages` 数组中
- [ ] 三页跳转与数据传递验证：首页 → 搜索页（globalData 传已选食材）、搜索页 → 详情页（query 参数传菜谱 ID）
- [ ] 移除旧的 `pages/example/index` 页面注册（已不用的模板页）

### 输入
- 现有 `app.json`、`app.wxss`、`app.js`
- 三页现有代码（index / search / detail）

### 输出文件
- `miniprogram/app.wxss` — 更新，添加全局色板
- `miniprogram/app.json` — 更新导航栏 + 移除 example 页

### 红线约束
- 不修改 `data/` 和 `utils/` 目录
- 不使用云开发 API
- 不改变三页现有功能逻辑

---

## 第二层：任务清单

| 编号 | 任务 | 完成标准 |
|------|------|---------|
| 7.1 | 更新 `app.wxss` 全局色板 | 注释色板完整，覆盖所有关键色值 |
| 7.2 | 更新 `app.json` 导航栏和页面注册 | 导航栏暖白、example 页移除、三业务页验证 |
| 7.3 | 验证三页跳转数据流 | 首页 globalData 写 → 搜索页读 → 详情页 query id 读，链路完整 |

---

## 第三层：任务追加

（无）

---

## 第四层：Scout 摘要

> 跳过。已知：
> - `app.json` 当前注册：`pages/index/index`、`pages/example/index`、`pages/search/search`、`pages/detail/detail`
> - `app.wxss` 当前：全局 `.container` + `button` + `page` 样式
> - `app.js` globalData 已有 `selectedIngredients: []`
